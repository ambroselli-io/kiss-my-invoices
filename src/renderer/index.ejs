<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="Content-Security-Policy" content="script-src 'self' 'unsafe-inline'" />
    <script defer data-domain="invoices-electron.kiss-my.app" data-api="https://plausible.io/api/event">
      !(function () {
        ("use strict");
        var a = window.location,
          r = window.document,
          o = r.currentScript,
          l = o.getAttribute("data-api") || new URL(o.src).origin + "/api/event";
        function s(e) {
          console.warn("Ignoring Event: " + e);
        }
        function e(e, t) {
          // if (/^localhost$|^127(\.[0-9]+){0,2}\.[0-9]+$|^\[::1?\]$/.test(a.hostname) || "file:" === a.protocol) {
          //   console.log("lida");
          //   return s("localhost");
          // }
          if (!(window._phantom || window.__nightmare || window.navigator.webdriver || window.Cypress)) {
            try {
              console.log("lidu");
              if ("true" === window.localStorage.plausible_ignore) return s("localStorage flag");
            } catch (e) {
              console.warn("localStorage not available");
            }
            var i = {},
              n =
                ((i.n = e),
                (i.u = a.href),
                (i.d = o.getAttribute("data-domain")),
                (i.r = r.referrer || null),
                t && t.meta && (i.m = JSON.stringify(t.meta)),
                t && t.props && (i.p = t.props),
                (i.h = 1),
                new XMLHttpRequest());
            n.open("POST", l, !0),
              n.setRequestHeader("Content-Type", "text/plain"),
              n.send(JSON.stringify(i)),
              (n.onreadystatechange = function () {
                4 === n.readyState && t && t.callback && t.callback();
              });
          }
        }
        var t = (window.plausible && window.plausible.q) || [];
        window.plausible = e;
        for (var i, n = 0; n < t.length; n++) e.apply(this, t[n]);
        function c() {
          (i = a.pathname), e("pageview");
        }
        window.addEventListener("hashchange", c),
          "prerender" === r.visibilityState
            ? r.addEventListener("visibilitychange", function () {
                i || "visible" !== r.visibilityState || c();
              })
            : c();
      })();
    </script>
    <title>💋 Kiss my Invoices!</title>
  </head>
  <body>
    <div id="notification" class="hidden">
      <p id="message"></p>
      <button id="close-button" onClick="closeNotification()">Close</button>
      <button id="restart-button" onClick="restartApp()" class="hidden">Restart</button>
    </div>
    <div id="root"></div>
  </body>
</html>
